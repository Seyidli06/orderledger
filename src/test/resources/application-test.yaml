spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:postgresql://localhost:5432/orderledger_test}
    username: ${TEST_DB_USERNAME:postgres}
    password: ${TEST_DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true