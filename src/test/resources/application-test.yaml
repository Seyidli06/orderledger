spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orderledger_test
    username: postgres
    password: 12345

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true